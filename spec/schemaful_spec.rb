# frozen_string_literal: true

require 'spec_helper'

describe Schemaful do
  it 'has a version number' do
    expect(Schemaful::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
